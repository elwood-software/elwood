import type {PropsWithChildren} from 'react'

export function {{ pascalCase name }}(props: PropsWithChildren): JSX.Element {
  return (
    <div>
      <h1>{{ pascalCase name }} Component</h1>
      {props.children}
    </div>
  );
};
